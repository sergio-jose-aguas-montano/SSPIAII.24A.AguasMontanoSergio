#Librerias
if(!require(ggplot2)){
  install.packages("ggplot2", dependencies = T)}

if(!require(caTools)){
  install.packages("caTools", dependencies = T)}

#Habilitar
library(ggplot2)
library(caTools)